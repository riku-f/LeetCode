class Solution(object):
    def isPalindrome(self, x):
        l = len(str(x))
        if str(x)[:len(str(x))//2]==str(x)[::-1][:len(str(x))//2]:
            return True
        else:
            return False
        """
        :type x: int
        :rtype: bool
        """
        
